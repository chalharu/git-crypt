[package]
name = "git-crypt"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.54", features = ["derive"] }
git2 = { version = "0.21.0", git = "https://github.com/chalharu/git2-rs.git" }
hex = "0.4.3"
pgp = "0.18.0"
rand = "0.8.5" # pgpで使われているrandのバージョンが0.8.5のため、合わせる必要がある。
regex = "1.12.2"
thiserror = "2.0.18"
